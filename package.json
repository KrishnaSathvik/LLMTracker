{
  "name": "llm-tracker",
  "version": "1.0.0",
  "description": "A production-ready system for tracking and analyzing LLM interactions",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd services/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "build": "npm run build:api && npm run build:web",
    "build:api": "cd services/api && npm run build",
    "build:web": "cd apps/web && npm run build",
    "start": "cd services/api && npm start",
    "install:all": "npm install && cd services/api && npm install && cd ../../apps/web && npm install && cd ../../apps/extension && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
